apiVersion: v2
name: bn-auth
description: BN Project Helm Charts Keycloak
type: application

# Helm chart version
version: 1.0.9

# BN Project version
appVersion: "0.0.1"

dependencies:
  - name: keycloak
    version: 26.1.4
    repository: https://charts.bitnami.com/bitnami

